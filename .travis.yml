language: ruby
rvm:
  - 2.5.8
bundler_args: --without production
services:
  - xvfb
script:
  - bundle exec rake db:schema:load
  - bundle exec rake db:test:prepare
  - bundle exec rails test
  - bundle exec cucumber || bundle exec cucumber -p rerun
  #- cat tmp/capybara/*html
