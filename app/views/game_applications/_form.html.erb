<%= form_with model: [@game_application.game, @game_application], local: false do |f| %>
    <%= f.error_messages :header_message => "Failed to save application details", :message => "The following problems prevented your details from being saved:" %>

	<%= f.hidden_field :user_id %>
	<%= f.hidden_field :game_id %>

	<p>You are applying to run the game on <%= @game_application.game.date_range %>.</p>
		<%= f.label :details, "Please enter any supporting details for your application." %>
    Click <%= link_to "here", "/pages/196", { :target => '_new' } %> to view markup options. Raw HTML is prohibited.<br />
		<%= f.text_area :details, id: :game_application_details %>
	</p>
<% end %>
