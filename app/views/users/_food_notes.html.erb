<% set_content_for :name do %>
    Food Notes
<% end %>

<% set_content_for :actions do %>
    <% if is_owner or is_admin %>
        [ <%= link_to "Edit", edit_emergency_details_user_path(user), :method => :get, :remote => true %> ]
    <% end %>
<% end %>

<%= RedCloth.new( "#{user.food_notes}", [:filter_html] ).to_html.html_safe %>
<%= "&nbsp;".html_safe if user.food_notes.nil? or user.food_notes.strip.empty? %>